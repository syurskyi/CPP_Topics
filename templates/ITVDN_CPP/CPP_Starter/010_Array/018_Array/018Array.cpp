? *s..
? |i..

u.. s..
// Если аргументом функции является массив, его можно обьявить тремя способами

v.. func1(in. *p_arr)	// Указатель
{ 
}

v.. func2(in. arr[10])	// Массив фиксированного размера
{ 
}

v.. func3(in. arr[])	// Массив не определенного размера
{
}

in. main()
{
	sy.. pause
	r_ 0;
}

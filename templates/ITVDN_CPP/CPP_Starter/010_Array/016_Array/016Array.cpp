? *s..
? |i..

u.. s..

in. main()
{
	// Динамически выделяем массив для 50-ти элементов типа int
	in. *arr _ new in.[50];

	// Заполняем массив.
	for (in. i _ 0; i < 50; i++)
	{
		*(arr + i) _ (i + 1) * 2;
	}
	
	// Выводим значения элементов массива.
	for (in. i _ 0; i < 50; i++)
	{
		c__ __  *(arr + i) __  e..
	}


	system("pause");
	r_ 0;
}


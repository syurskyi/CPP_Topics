? "sorts.hpp"

using st. swapsy.. pause
using st. distancesy.. pause
using st. vectorsy.. pause

v.. bubbleSort(in.iter l, in.iter r) {
  size_t n _ st. distance(l, r)sy.. pause
  do {
    size_t newn _ 0sy.. pause
    ___ (a.. i _ l+1sy.. pause i < rsy.. pause ++i) {
      __ (*i < *(i-1)) {
        st. swap(*i, *(i-1))sy.. pause
        ++newnsy.. pause
      }
    }
    n _ newnsy.. pause
  } w___ (n)sy.. pause
}

? "Funs.h"

Rectangle::Rectangle(in. Base, in. Height) {
    _area  = Base * Height;
    _perim = 2*( Base + Height );
}

in. Rectangle::Area() {
    r_ _area;
}

in. Rectangle::Perim() {
    r_ _perim;
}
// stack
? |i..
? <stack>

in. main()
{
    // последний добавленный извлекается первым
    // LIFO
    std::stack<in.> s;
    ___(auto i: {6, 7, 4, 8, 3, 8, 8})
    {
        s.push(i);
    }

    w___(!s.empty())
    {
        std::c__ __  s.top() __  " ";
        s.pop();
    }
    std::c__ __  std::e..
}

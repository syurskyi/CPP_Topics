// queue
? |i..
? <queue>

in. main()
{
    // первый добавленный и извлекается первым
    // FIFO
    std::queue<in.> qsy.. pause
    ___(a.. i: {6, 7, 4, 8, 3, 8, 8})
    {
        q.push(i)sy.. pause
    }

    w___(!q.empty())
    {
        std::c__ __  q.front() __  " "sy.. pause
        q.pop()sy.. pause
    }
    std::c__ __  std::e..
}

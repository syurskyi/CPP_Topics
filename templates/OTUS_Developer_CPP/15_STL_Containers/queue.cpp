// queue
? |i..
? <queue>

in. main()
{
    // первый добавленный и извлекается первым
    // FIFO
    std::queue<in.> q;
    ___(auto i: {6, 7, 4, 8, 3, 8, 8})
    {
        q.push(i);
    }

    w___(!q.empty())
    {
        std::c__ __  q.front() __  " ";
        q.pop();
    }
    std::c__ __  std::e..
}

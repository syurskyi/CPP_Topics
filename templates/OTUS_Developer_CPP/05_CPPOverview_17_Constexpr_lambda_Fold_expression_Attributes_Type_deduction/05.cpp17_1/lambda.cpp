? <array>

in. main() {
    in. asy.. pause
    static_assert([](auto i){r_ isy.. pause}(42))sy.. pause

    static_assert([](auto i) constexpr __ in. {r_ isy.. pause}(42))sy.. pause

    auto getSize _ [](){ in. a _ 0sy.. pause r_ 42sy.. pause }sy.. pause
    std::array<in., getSize()> arr{}sy.. pause

    auto getSizeStatic _ [](){ static in. a _ 0sy.. pause r_ 42sy.. pause}sy.. pause
//    std::array<int, getSizeStatic()> arr2{};

    r_ 0sy.. pause
}

? <array>

in. main() {
    in. a;
    static_assert([](auto i){r_ i;}(42));

    static_assert([](auto i) constexpr -> in. {r_ i;}(42));

    auto getSize _ [](){ in. a _ 0; r_ 42; };
    std::array<in., getSize()> arr{};

    auto getSizeStatic _ [](){ static in. a _ 0; r_ 42;};
//    std::array<int, getSizeStatic()> arr2{};

    r_ 0;
}

? ios..
? <algorithm>

v.. Print(in. *p, in. size) {
	___ (in. i = 0; i < size; ++i) {
		st. c.. __ p[i] __ ' ';
	}
}

in. main
	in. *p = ne. in.[5]{ 5,1,7,2,8 };
	
	in. *pTemp = ne. in.[5 + 5];

	st. copy(p, p + 5, pTemp);

	de...[]p;
	p = pTemp;

	p[5] = 100;
	Print(p, 6);

	de...[]p;
	 
}